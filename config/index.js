const genesisBlockDevnet = require("./genesis_block");
const configDevnet = require("./config");
const { CONSTANTS } = require("./constants");

module.exports = {
  configDevnet,
  genesisBlockDevnet,
  CONSTANTS
};
